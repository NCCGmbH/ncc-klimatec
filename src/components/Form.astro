---
type Props = {
  fid?: string;
  fmethod?: string;
}

const { fid, fmethod }: Props = Astro.props;
---

<form 
  id={fid ?? undefined} 
  method={fmethod ?? undefined} 
  class="w-full flex flex-col max-h-fit gap-4 bg-white shadow-lg p-4 rounded-md"
>
  <slot />
</form>
