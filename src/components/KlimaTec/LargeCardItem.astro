---

type Props = {
  title: string
};

const { title }: Props = Astro.props;

---

<div class="flex flex-col gap-2">
  <div class="flex [&>*]:size-12 justify-center">
    <slot name="icon" />
  </div>
  <p class="font-bold">{title}</p>
  <slot />
</div>