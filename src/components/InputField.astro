---

type Props = {
  itype?: astroHTML.JSX.HTMLInputTypeAttribute | "textarea";
  id?: string;
  name?: string;
  required?: boolean;
  autofocus?: boolean;
};

const { itype, id, name, required=false, autofocus=false }: Props = Astro.props;
---

<label>
  <div>
    <slot />
  </div>
  {itype === "textarea" ? (
    <textarea
      autofocus={autofocus}
      id={id}
      name={name ?? undefined}
      required={required ? true : undefined}
      class="w-full h-24 max-h-64 focus:ring-sky-400 focus:ring-2 outline-none rounded-md px-1 bg-[#ebeeef]"
    />
  ) : (
    <input
      autofocus={autofocus}
      id={id}
      name={name ?? undefined}
      type={itype ?? undefined}
      required={required ? true : undefined}
      class="max-w-64 w-full focus:ring-sky-400 focus:ring-2 outline-none rounded-md px-1 bg-[#ebeeef]"
    />
  )}
</label>
